pipeline {
	agent any
	stages {
		stage("Cleaning Stage") {
			steps {
				bat "mvn clean"
			}
		}
		stage("Building Stage") {
			steps {
				bat "mvn clean"
			}
		}
		stage("Packaging Stage") {
			steps {
				bat "mvn clean"
			}
		}
	}
}